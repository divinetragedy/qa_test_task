{
  "name": "qa-test-task",
  "version": "0.0.1",
  "scripts": {
    "test": "playwright test",
    "ctest": "playwright test --project=chromium",
    "ftest": "playwright test --project=firefox",
    "wtest": "playwright test --project=webkit"
  },
  "keywords": [
    "playwright",
    "qa",
    "test-task"
  ],
  "author": "divinetragedy",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.38.0"
  }
}

